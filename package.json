{
  "name": "just-api",
  "scripts": {
    "dev": "bun run --hot api/app.ts"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.6.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  }
}